{"timestamp":"1726703089","author":"🐱","location":{"commit":"fa1be9d41556","path":"hexdump.c","range":{"startLine":1}},"description":"This presumably includes our header, what is the difference between\nthe `#include \"...\"` and `#include \u003c...\u003e` lines? We have used the\nformer on our own header, the latter on a system header, is that it?\n"}

{"timestamp":"1726703509","author":"🐶","parent":"3e5249a88192c1b37d7f88969e13dee829bbc90f","location":{"commit":"fa1be9d41556","range":{"startLine":0}},"description":"Yes that's the idea, you can add paths to either the user or system\nheader locations, but the default system headers include the \n[standard library](https://en.cppreference.com/w/c/header)\nand installed software (if it has headers), and the current\ndirectory is always searched for user files.\n"}

{"timestamp":"1726704087","author":"🐶","location":{"commit":"fa1be9d41556","path":"hexdump.c","range":{"startLine":4}},"description":"This includes the fixed width integer types. Previously we have used\n`int`, which is the \"most optimal type for the platform\". Usually it\nis 32-bits, but it is not given. Whereas `stdint.h` includes types\nlike `int8_t`, `int16_t`, `int32_t` and `int64_t`, which are defined\nto be 8, 16 and 32-bits. There are also unsigned counterparts\n`uint8_t`, `uint16_t`, `uint32_t` and `uint64_t`.\n\nUnsigned means not negative, so zero or higher."}

{"timestamp":"1726704640","author":"🐱","parent":"cd141323da24cb437030d294c5582fac576b3e46","location":{"commit":"fa1be9d41556","range":{"startLine":0}},"description":"And I recall a bit being either `0` or `1`, so an 8-bit integer is\n`0000 0000`, `0000 0001`, `0000 0010`, `0000 0011`, ..., `1111 1110`,\n`1111 1111`. Just a second, that's from zero to 2^7 + 2^6 + 2^5 + 2^4\n+ 2^3 + 2^2 + 2^1 + 2^0 = 128 + 64 + 32 + 16 + 8 + 4 + 2 + 1 = 255. Or\nat least 255 possible values, if we have signed integers then some of\nthose values are going to be negative I assume.\n\nAlso, I recall that a byte is another word for an 8-bit value.\n"}
